		ROCKETSEAT
VERSIONAMENTO DE CÓDIGO - GIT HUB

GIT INIT --> INICIA O GIT (REPOSITORIO) NO SEU PROJETO
GIT STATUS --> VERIFICAR ALTERAÇÕES DE PASTAS E ARQUIVOS NO PROJETO
GIT ADD . -->ADICIONA TODOS OS ARQUIVOS E PASTAS MODIFICADOS, AO STAGE AREA
GIT COMMIT -M "MESSAGE HERE" --> CRIA E DESCREVE UM PONTO NA HISTÓRIA
GIT LOG -->HISTÓRICO DE COMMIT DO PROJETO 
 	aula 02
Como criar um repositório
- git init --> Inicia um repositório
- git status --> oque tem no repositório
- pwd --> ver em que repositorio estamos
- git add documento --> adiciona o documento selecionado para staging area, e em caso de modificação do documento será mostrado na tela
- git rm --cached ."nomedodocumento" --> remove o documento do staging area
- git restore --> volta o arquivo como estava anteriormente, antes da modificação
- git commit -m "initial commit" --> como iniciar um commit

commit é o
Ato de salvar permanentemente um conjunto de mudanças feitas em um projeto,
como um ponto de salvamento em um "diário" do código, usando um sistema de controle de versão como o Git

			aula 03
git log --> analisar a linha de tempo dos commits
git checkout --> analisa um commit especifico da linha de tempo